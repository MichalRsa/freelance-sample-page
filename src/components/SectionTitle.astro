---
import { twMerge } from "tailwind-merge";

interface Props {
  title: string;
  classes?: string;
}

const { title, classes } = Astro.props;
---

<h2 class={twMerge("mb-8 text-xl md:text-4xl", classes)}>
  {title}
</h2>
