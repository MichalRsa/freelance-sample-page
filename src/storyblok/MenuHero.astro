---
import { storyblokEditable } from "@storyblok/astro";
import StoryblokComponent from "@storyblok/astro/StoryblokComponent.astro";
import type { MenuHeroStoryblok } from "../../component-types-sb";

interface Props {
  blok: MenuHeroStoryblok;
  language: string;
}

const { blok, language } = Astro.props;
---

<div {...storyblokEditable(blok)} class="bg-secondary-500 p-20">
  <div
    class="from-black/70 via-black/50 to-transparent z-10 bg-gradient-to-t py-6"
  >
    <h1 class="mb-10 text-center text-5xl font-bold text-text-500">
      {blok.title}
    </h1>
    {
      !!blok.links && (
        <div class="mx-auto max-w-96">
          <div class="flex justify-around">
            {blok.links.map((blok: any) => (
              <StoryblokComponent
                blok={{
                  ...blok,
                  fontSize: "text-2xl",
                  color: "black",
                  font: "lora",
                }}
                language={language}
              />
            ))}
          </div>
        </div>
      )
    }
  </div>
</div>
